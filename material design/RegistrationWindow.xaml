<Window x:Class="material_design.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Регистрация нового пользователя" 
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Поля ввода -->
        <TextBox Grid.Row="1" x:Name="UsernameTextBox" 
                 Tag="Логин"
                 GotFocus="TextBox_GotFocus"
                 LostFocus="TextBox_LostFocus"
                 Margin="0,0,0,15" 
                 Padding="5" FontSize="14"/>

        <PasswordBox Grid.Row="2" x:Name="PasswordBox" 
                     Tag="Пароль"
                     GotFocus="PasswordBox_GotFocus"
                     LostFocus="PasswordBox_LostFocus"
                     Margin="0,0,0,25"
                     Padding="5" FontSize="14"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Зарегистрировать" 
                    Click="RegisterButton_Click" 
                    Margin="0,0,10,0"
                    Width="150" Height="30"
                    Background="#FF3F51B5" Foreground="White"/>

            <Button Content="Отмена" 
                    Click="CancelButton_Click"
                    Width="100" Height="30"
                    BorderBrush="#FF3F51B5" BorderThickness="1"/>
        </StackPanel>

        <!-- Сообщение об ошибке -->
        <TextBlock x:Name="ErrorMessageText" Grid.Row="5" 
                   Foreground="Red"
                   TextWrapping="Wrap" 
                   Margin="0,10,0,0" 
                   Visibility="Collapsed"
                   FontSize="12"/>
    </Grid>
</Window>