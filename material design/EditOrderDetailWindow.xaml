<Window x:Class="material_design.EditOrderDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование детали заказа" Height="350" Width="400">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Заказ:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" Name="cmbOrder" DisplayMemberPath="id_order" 
                  SelectedValuePath="id_order" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Позиция меню:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="3" Name="cmbMenuItem" DisplayMemberPath="item_name" 
                  SelectedValuePath="id_menu_item" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Количество:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="5" Name="txtQuantity" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="Цена за единицу:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="7" Name="txtUnitPrice" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>