<Window x:Class="material_design.autorization"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Авторизация" 
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Поля ввода -->
        <TextBox Grid.Row="1" Name="tBL" 
                 Tag="Логин"
                 GotFocus="TextBox_GotFocus"
                 LostFocus="TextBox_LostFocus"
                 Margin="0,0,0,15" Width="300"
                 Padding="5" FontSize="14"/>

        <PasswordBox Grid.Row="2" Name="tBP" 
                     Tag="Пароль"
                     GotFocus="PasswordBox_GotFocus"
                     LostFocus="PasswordBox_LostFocus"
                     Margin="0,0,0,25" Width="300"
                     Padding="5" FontSize="14"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Войти" 
                    Click="Button_Click" 
                    Margin="0,0,10,0"
                    Width="120" Height="30"
                    Background="#FF3F51B5" Foreground="White"/>

            <Button Content="Отмена"
                    Width="120" Height="30"
                    Background="LightGray"/>
        </StackPanel>

        <Button Grid.Row="4" Content="Регистрация" 
                Click="RegisterButton_Click" 
                HorizontalAlignment="Center"
                Width="150" Height="30"
                Background="Transparent" BorderBrush="#FF3F51B5"/>
    </Grid>
</Window>