<Window x:Class="material_design.EditEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование сотрудника" Height="350" Width="400">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ФИО сотрудника:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Name="txtName" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Телефон:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Name="txtPhone" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Должность:" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="5" Name="cmbPost" DisplayMemberPath="title_post" 
                  SelectedValuePath="id_post" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="Email:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="7" Name="txtEmail" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="Save_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>